<div class="jumbotron">
  <h1><i class="fa fa-bell"></i> 401 <small>(Unauthorized)</small></h1>

  {% if req.session.id %}
    <p>You don't seem to have access to this page. Tough luck.</p>
  {% else %}
    <p>It looks like you're not authorized to view this page. If you are not logged in, you can login now and try again.</p>

    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <form action="/login" method="POST">
          <div class="form-group">
            <input type="text" class="form-control input-lg" placeholder="Username..." name="username" id="username" required autofocus>
          </div>
          <div class="form-group">
            <input type="password" class="form-control input-lg" placeholder="Password..." name="password" id="password" required>
          </div>
          <input type="hidden" name="return-to" value="{{req.url.path}}" />
          <button class="btn btn-lg btn-theme btn-block" type="submit">Login</button>
        </form>
      </div>
    </div>
  {% endif %}
</div>
